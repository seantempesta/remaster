# Core - MUST install from cu121 index:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Then install the rest from this file:
#   pip install -r requirements.txt

# Pin torch to prevent CPU-only overwrite
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# Real-ESRGAN + basicsr
realesrgan
basicsr

# Video Depth Anything deps
huggingface_hub

# RAFT deps
scipy
opencv-python

# Utilities
imageio-ffmpeg
numpy
pillow
